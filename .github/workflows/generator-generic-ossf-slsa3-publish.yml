# This workflow is disabled - SLSA provenance generation not needed for this project
# To enable: Configure proper artifact generation and SLSA requirements

name: SLSA generic generator

on:
  workflow_dispatch: # Manual trigger only

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "This workflow is disabled - SLSA provenance generation not configured"
