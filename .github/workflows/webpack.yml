# This workflow is disabled - webpack not configured in project
# To enable: Add package.json and webpack configuration

name: NodeJS with Webpack

on:
  workflow_dispatch: # Manual trigger only, never runs automatically

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "This workflow is disabled - webpack not configured in project"
