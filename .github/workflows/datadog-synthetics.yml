# This workflow is disabled - requires Datadog API keys
# To enable: Add DATADOG_API_KEY and DATADOG_APP_KEY secrets to repository
name: Run Datadog Synthetic tests
on:
  workflow_dispatch: # Manual trigger only, never runs automatically
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "This workflow is disabled"
